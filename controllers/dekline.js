// Require the express package and use express.Router()
const express = require('express');
const router = express.Router();

// this is a generic api controller that is currently not used

module.exports = router;
